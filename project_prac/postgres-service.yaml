---
apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: votingapp
    name: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: votingapp
    name: postgres
