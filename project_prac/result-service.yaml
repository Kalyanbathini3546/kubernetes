---
apiVersion: v1
kind: Service
metadata:
  name: result-dep
  labels:
    app: votingapp
    name: result
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: votingapp
    name: result
